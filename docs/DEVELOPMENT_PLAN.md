# План разработки RLM (Development Plan)

## 1. Общая информация

**Цель проекта:** Создать инструмент, позволяющий агроному просматривать спутниковые снимки полей в различных спектрах (RGB, False Color, NDVI, NDWI и др.), с автоматическим наложением контура поля и возможностью LLM-анализа.

**Ключевые требования:**
- Буферная зона 500 метров от границы поля (выносится в параметр).
- Поиск малооблачных сцен Sentinel-2 Level-2A.
- Выбор даты и типа продукта.
- Визуализация с наложением векторного контура поля.
- Интеграция с MCP и Qwen3 (OpenRouter).

---

## 2. Этапы разработки

### Этап 0: Документация (завершён)

**Что должно быть сделано:**
- [x] `docs/README.md`
- [x] `docs/DEVELOPMENT_PLAN.md`
- [x] `docs/ARCHITECTURE.md`
- [ ] `docs/API.md` — отложен (API документируется в docstring и MCP-схеме)
- [ ] `docs/USER_STORIES.md` — отложен

**Тесты и проверка:**
- Проверка корректности документации (читаемость, полнота, соответствие ТЗ).
- **Как проверить:** Ручной review + проверка, что все ссылки и названия модулей актуальны.

### Этап 1: Подготовка и рефакторинг (завершён)

**Что было сделано:**
- Созданы `config.py`, `models.py`, `search.py`, `processor.py`, `cli.py`
- Реализована функция создания буфера 500 м с корректной проекцией
- Обновлены MCP-инструменты и CLI
- Обновлены зависимости и entry-point

**Тесты и проверка корректности:**
- **Unit-тесты:** `test_create_buffer()` — проверка площади буфера, корректности CRS, отсутствия самопересечений.
- **Интеграционные тесты:** `test_list_scenes()` — проверка, что при валидном KML возвращается список сцен (мокирование API).
- **Как проверить:** Запустить `rlm search tests/fixtures/test_field.kml` и убедиться, что буфер создан, поиск отрабатывает, возвращаются метаданные с `cloud_cover`.
- **Пользовательское тестирование:** CLI-команды доступны для тестирования.

### Этап 2: Обработка и расчёт индексов (завершён)

**Что было сделано:**
- [x] Доработан `processor.py` (оркестратор: буфер → поиск → индексы → отчёт)
- [x] Создан `indices.py` — расчёт NDVI, NDWI, визуализация RGB + NDVI с контуром
- [x] Создан `sentinel_filter.py` — двухэтапная фильтрация: STAC pre-filter + SCL проверка по полю
- [x] Создан `downloader.py` — доступ к COG-файлам Sentinel-2 L2A
- [x] Реализована многосценовая обработка (`process_multiple_scenes`)

**Тесты и проверка корректности:**
- **Unit-тесты:** `test_calculate_ndvi()`, `test_apply_scl_mask()`, `test_crop_to_buffer()`, `test_filter_pipeline()`.
- **Алгоритмические тесты:** Сравнение рассчитанных индексов с эталонными значениями на синтетических данных (NDVI в диапазоне [-1, 1]).
- **Как проверить:** Запустить тесты `pytest tests/test_processor.py`. Проверить, что после обработки растра значения NDVI соответствуют ожидаемым для вегетации (0.4–0.9).
- **Пользовательское тестирование:** Агроном может проверить корректность маски облачности и индексов на реальных полях.

### Этап 3: Интерфейсы и интеграция (завершён)

**Что было сделано:**
- [x] CLI (`rlm search`, `rlm analyze`) — реализован через `typer`
- [x] MCP-сервер (инструменты `list_available_scenes`, `analyze_field`) — реализован через `FastMCP`
- [x] Интеграция LLM-анализа (Qwen3 через LiteLLM + OpenRouter)

**Тесты и проверка корректности:**
- **Unit-тесты:** Тесты CLI (invoke), тесты MCP-инструментов.
- **Интеграционные тесты:** `test_end_to_end_analysis()` — от KML до получения LLM-ответа.
- **Как проверить:** Убедиться, что `create_buffer` правильно расширяет геометрию, LLM возвращает осмысленные рекомендации.
- **Пользовательское тестирование:** Агрономы могут тестировать CLI-команды и качество рекомендаций от Qwen3 на реальных полях.

### Этап 4: Веб-интерфейс и продвинутые возможности (в планах)

**Задачи:**
- [ ] Streamlit / Gradio веб-интерфейс
- [ ] Таймлапс, сравнение дат
- [ ] Интеграция модели прогноза урожайности (см. `docs/presCropYieldPrediction.md`)
- [ ] Автоматическое выделение проблемных зон на полях

**Тесты и проверка:**
- **Unit / Integration тесты** для новых компонентов.
- **Пользовательское тестирование:** Агрономы и пользователи 1С:ЕРП АПК тестируют удобство интерфейса, точность рекомендаций и прогнозов.

### Этап 5: Тестирование, документация и релиз (финальный)

**Задачи:**
- [ ] Полный набор unit, integration и e2e-тестов
- [ ] Обновление всей документации
- [ ] Примеры использования и туториалы
- [ ] Подготовка к релизу (pip, Docker, GitHub)

**Тесты и проверка:**
- Покрытие кода тестами > 80% для core-модулей (`processor`, `search`, `llm`).
- **Как проверить:** Запуск `pytest` + `pytest --cov`.
- **Пользовательское тестирование:** Полноценное бета-тестирование с реальными пользователями (агрономы, специалисты хозяйств). Оценка usability, точности индексов и качества LLM-рекомендаций.

---

## 3. Технологический стек (обновлённый)

- **Язык:** Python 3.10+
- **Спутниковые данные:** `pystac-client` (STAC API Earth Search), `sentinelsat` (legacy), `rasterio`, `rioxarray`
- **Геоданные:** `geopandas`, `shapely`, `pyproj`, `mgrs`
- **Визуализация:** `matplotlib`, `rasterio.plot`
- **LLM:** `litellm` + OpenRouter (Qwen3-70B)
- **MCP:** `mcp` сервер (FastMCP)
- **CLI:** `typer`
- **Конфигурация:** `pydantic-settings`, `.env`

---

## 4. Текущий статус

### Реализовано
- [x] Документация (README, ARCHITECTURE, DEVELOPMENT_PLAN)
- [x] Конфигурация через `.env` + `pydantic-settings`
- [x] Pydantic-модели (SearchRequest, SceneMetadata, AnalysisResult)
- [x] CLI-интерфейс (поиск, анализ)
- [x] MCP-сервер (инструменты: `list_available_scenes`, `analyze_field`)
- [x] Создание буферной зоны 500м (shapely + geopandas)
- [x] Поиск сцен через STAC API (pystac-client, Earth Search)
- [x] Расчёт NDVI, NDWI
- [x] Визуализация: RGB + NDVI + contour overlay
- [x] Обработка облачности (SCL mask) - двухэтапная фильтрация
- [x] Интеграция с Qwen3 через LiteLLM + OpenRouter
- [x] Многосценовая обработка
- [x] Двухэтапная SCL-фильтрация (STAC + SCL mask)
- [x] E2E-тесты: фильтрация по месяцам 2025

### В плане
- [ ] Веб-интерфейс (Streamlit / Gradio)
- [ ] Таймлапс-сравнение дат
- [ ] Автоматическое выделение проблемных зон на полях
- [ ] Модель прогноза урожайности (ML)
- [ ] Docker-образ
- [ ] Релиз (pip, GitHub)

---

**Статус:** Документация обновлена (актуализирована под версию 0.2.0).